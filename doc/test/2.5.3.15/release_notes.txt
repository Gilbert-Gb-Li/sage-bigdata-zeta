注：超出以下范围的功能不保证能工作

功能范围：
一、安装： 
     1.手动安装
     2.linux支持master + worker（主要验证redhat7.2）
	   
二、数据（解析）通道：
     1.本地文件或目录作为数据源
	 2.kafka作为数据源  
     3.网络作为数据源
     4.ES2作为数据存储
	 5.网络作为数据存储

三、解析规则：
     1.正则类型
	 2.json类型
	 
四：分析规则：
     1.SQL类型
     
	 
	 
已知问题：
#742 运行存储至es2的数据通道，es2异常又恢复之后，存储得到的数据条数有多余 
#734 数据源为本地文件csv时，往读取过的文件中追加内容，读取到的条数错误（2.6解决）
#732 数据存储为网络类型(tcp)时，运行数据通道得到的结果错误（2.6解决）
#733 数据源为本地文件或目录，无表头的excel/csv，保存成功后刷新页面，再次打开，“是否有表头”变为有（2.6解决）
#738 页面停止并删除woker，处于采集器已关闭或者不存在状态时，数据通道的"启动/停止"对应图标为启动状态（2.6解决）
#739 分析规则中含有时间类型的字段，运行分析通道，得到的时间类型字段的值不是时间类型 
#730 2.5.x分支采集网络UDP数据会出现Queue full Exception
#672 解析通道 kafka到ES5， 运行一段时间后，ES5出错，导致通道出错，但ES5恢复后通道无法恢复（2.6解决） 
#284 数据通道（本地excel文件夹到kafka）运行一个多小时后， 新加入的excel文件不会再被读取 
#356 分析规则中指定字段为解析规则中没有的字段，运行数据通道，未有任何提示信息 
#281 数据通道（本地excel文件到kafka）运行一段时间后，往本地目录下新增多个文件，读取到部分文件的数据条数为0 
#305 master异常终止，重启后，worker不能重新连上master 
#298 Kafka读取超时，导致只有部分数据存入ES2，但UI上无任何错误提示 
#236 使用非法的json文件(数据源)上传后无法保存
#376 数据通道绑定的采集器已关闭，或者不存在时，无法删除相关数据通道
#348 数据通道/数据源 的资产类型为空，导致数据通道不能被修改，也会影响运行
#293 创建好的数据源的资产类型为空
#376 数据通道绑定的采集器已关闭，或者不存在时，无法删除相关数据通道
#243 当数据源来源是已经格式化好的数据时，`添加原始数据`没有效果	   
#340 采集器后台进程存在，页面采集器状态已关闭或者不存在，采集器不可用（最近暂未复现该问题）
#329 当监控本地文件目录时,会有内存泄漏    
#230 复制数据通道时，默认会把底下的4个选项打开，而在原数据通道中这四项是关闭的 （2.6解决）
#385 配置分析通道底下的5个按钮(除cpu降频外)为“是”时，分析得到的结果中没有相应的字段信息(2.6解决)

   


 