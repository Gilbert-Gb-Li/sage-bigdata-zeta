package com.haima.sage.bigdata.etl.utils

import com.haima.sage.bigdata.etl.common.base.LogReader
import com.haima.sage.bigdata.etl.common.model._
import org.junit.Test

class TypeMatchCaseTest {

  /*@Test
  def types: Unit ={
   assert( Some(Akka()) match {
     case Some(_: Akka | _: TCP | _: UDP) =>
       true
     case _ =>
       false
   })
    assert( Some(TCP()) match {
      case Some(_: Akka | _: TCP | _: UDP) =>
        true
      case _ =>
        false
    })

    assert(!( Some(Net()) match {
      case Some(_: Akka | _: TCP | _: UDP) =>
        true
      case _ =>
        false
    }))

  }*/

}
